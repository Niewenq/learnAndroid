<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoDisplay"
        tools:targetApi="31">
        <service
            android:name=".services.CalculatorBinderService"
            android:enabled="true"
            android:exported="true"
            android:permission="com.ieening.androidipcserver.permission.CalculatorBinderService">
            <intent-filter>
                <action android:name="com.ieening.androidipcserver.action.CaculatorBinderService" />
            </intent-filter>
        </service>
        <service
            android:name=".services.MyService"
            android:enabled="true"
            android:exported="true"
            android:permission="com.ieening.androidipcserver.myservice">
            <intent-filter>
                <action android:name="com.ieening.androidipcserver.action.AIDL_SERVICE" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:excludeFromRecents="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>